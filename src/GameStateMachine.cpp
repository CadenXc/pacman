#include "GameStateMachine.h"

#include <iostream>

namespace pacman
{

    /*
    GameStateMachine::GameStateMachine() {
        std::cout << "GameStateMachine's costructor" << std::endl;

    }

    GameStateMachine::~GameStateMachine() {
        std::cout << "GameStateMachine's destructor" << std::endl;

    }

    void GameStateMachine::PushState( GameState* gameState );
    {
        gameStats.push( gameStates );
    }

    void GameStateMachine::ChangeState( GameState* gameState )
    {
    }

    void GameStateMachine::PopState()
    {
        gameStates.pop();
    }

    void GameStateMachine::PopAllStates()
    {
        for (
    }

    void GameStateMachine::UpdateState() 
    {
        gameSates.back().Update();
    }

    void gamestatemachine::Renderstate()
    {
        gameSates.back().Render();
    }
    */
}
